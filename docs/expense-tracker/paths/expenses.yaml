get:
  description: To get expenses list.
  operationId: listExpenses
  security:
    - bearerAuth: []
  responses:
    "200":
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../schemas/expense.yaml"
  tags:
    - Expenses
